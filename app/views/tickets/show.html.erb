

<div class="container">
  <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
      <h2><%= @ticket.title %></h2>
      <p><%= @ticket.description %></p>
        <%= simple_form_for([@ticket, @ticket_technology ]) do |f| %>
          <%= f.input :technology_id, collection: Technology.all %>
        <%= f.submit %>
      <% end %>
    </div>
  </div>
</div>
