<div class= "container">
  <div class="row">
  <% @tickets.each do |ticket| %>
  <% unless ticket.solved? || ticket.active? %>
    <div class="col-xs-12 col-md-6">
      <div class="card">
        <div class="card-user" style="background-image: url(<%= ticket.student.github_avatar_url %>)"></div>
        <div class="card-main">
          <div class="card-header <%= ticket.status %>">
            <span><%= ticket.student.github_username %></span>
            <span><%= ticket.status %></span>
          </div>
          <div class="card-content">
            <div>
              <h3><%= ticket.title %></h3>
              <p> Created <%= (Time.now - ticket.created_at).to_i / 60 %> minutes ago.</p>
            </div>

            <div class="card-tags">
                <% ticket.technologies.each do |technology| %>
                <div class="tech-tag <%= technology.name.downcase %>-tag">
                  <%= technology.name %>
                </div>
                <% end %>
            </div>
          </div>
        </div>
        <%= link_to "", ticket_path(ticket), class: "card-link"  %>
      </div>
    </div>
  <% end %>
    <% end %>
  </div>
</div>




